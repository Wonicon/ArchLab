mips8_shift:

按权移位，每一权的选择器组组成一个模块，在接口布线时体现功能

宏定义，使得实例化变得简单，按权移位的表述有共性

用iverlog -E测试与处理结果，验证宏的正确性

TODO 考察<<操作符的生成结果
<<操作符生成了独立的移位器元件。
为了方便进行符号位扩展和循环移位，将这两种情况的移位器输入扩大为原来的两倍，通过连线准备好扩展的符号位和等待循环移入地低位。

移位操作符的方案是在所有情况移位结束后再选择输出。

而框图的方案则是在移位过程中进行进行功能的选择。

虽然按照电路框图设计的移位器最后也是生成选择器，
但是为了书写代码的方便，最后生成的选择器入扇数非常大，但是选择器数量减少。

如果不在移位过程中进行选择，那么选择只需要移和不移两种，入扇数能减半。
虽然最后还要额外再添加一个选择器，但是总的电路级数估计是减半的（需要计算验证！）
所以可能移位操作符的方案更好？但是移位为了扩展，有两个移位器是双倍位数输入的，这种大入扇数会不会对移位器的性能造成影响？
（选择器级数一样，但是大入扇的选择器内部的电路复杂度更高）。

进一步了解了拼接宏的用法，宏的拼接运算符，只是对宏的参数有效的！